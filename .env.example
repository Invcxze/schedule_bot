BOT_TOKEN=
# Overrides the source configured in config.toml.
GOOGLE_SHEET_URL=
# Optional: absolute path to another config file
# BOT_CONFIG=/path/to/config.toml
# Only for a private Google Sheet (never commit this JSON):
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Celery broker/result backend. Required for the scheduled refresh, change
# notifications, and the morning digest — see "Celery + Redis" in README.
REDIS_URL=redis://localhost:6379/0
